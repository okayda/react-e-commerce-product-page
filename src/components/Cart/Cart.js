import Modal from "../UI/Modal";
import Card from "../UI/Card";

const Cart = function () {
  return (
    <Modal targetClass="cart-container">
      <Card></Card>
    </Modal>
  );
};

export default Cart;
